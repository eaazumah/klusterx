import { Container } from '@mantine/core'
import styled from 'styled-components'

const Fixtures: React.FC = () => {
  return <Styled size='lg'></Styled>
}

const Styled = styled(Container)``

export default Fixtures
